*Structure du fichier points.map A RESPECTER OBLIGATOIREMENT*

Au tout début (ne pas oublier de le MAJ) :

```
nombre_de_points
```

Et pour chaque ligne :

```
ID nom_du_pt longitude_X latitude_Y
```

NE PAS METTRE DE LIGNE VIDE A LA FIN (peut apparaître quand on enregistre le fichier sur certains éditeurs)

*Structure du fichier steps.map A RESPECTER OBLIGATOIREMENT*

Au tout début (ne pas oublier de le MAJ) :

```
nombre_d_etapes
```

Puis :

```
ID_etape nom_etape nb_de_pts_dans_letape
```

Ce nombre compte les points à l'extrémité.
Et en dessous, autant de fois que la valeur de ce nombre :

```
point_1_de_l_etape point_2 ... point_n
```

NE PAS METTRE DE LIGNE VIDE A LA FIN (peut apparaître quand on enregistre le fichier sur certains éditeurs)

